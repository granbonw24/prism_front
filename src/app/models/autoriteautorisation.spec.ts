import { Autoriteautorisation } from './autoriteautorisation';

describe('Autoriteautorisation', () => {
  it('should create an instance', () => {
    expect(new Autoriteautorisation()).toBeTruthy();
  });
});
