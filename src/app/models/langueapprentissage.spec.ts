import { Langueapprentissage } from './langueapprentissage';

describe('Langueapprentissage', () => {
  it('should create an instance', () => {
    expect(new Langueapprentissage()).toBeTruthy();
  });
});
