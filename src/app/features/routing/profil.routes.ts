import { Routes } from '@angular/router';
import { ProfilComponent } from '@features/profil/profil.component';

export const profilFeatureRoutes: Routes = [{ path: 'profil', component: ProfilComponent }];
