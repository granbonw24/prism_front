import { Routes } from '@angular/router';
import { PromoteursComponent } from '@features/promoteurs/promoteurs.component';

export const promoteursFeatureRoutes: Routes = [{ path: 'promoteurs', component: PromoteursComponent }];
